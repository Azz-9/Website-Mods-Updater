class Mod {
	constructor(title, id, description, icon_url, slug, enabled) {
		this.title = title;
		this.id = id;
		this.description = description;
		this.icon_url = icon_url;
		this.slug = slug;
		this.enabled = enabled;
	}
}